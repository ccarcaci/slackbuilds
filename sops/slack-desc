# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

      |-----handy-ruler------------------------------------------------------|
sops: sops (Secrets OPerationS)
sops:
sops: sops is an editor of encrypted files that supports YAML, JSON, ENV,
sops: INI and BINARY formats and encrypts with AWS KMS, GCP KMS, Azure Key
sops: Vault, HuaweiCloud KMS, HashiCorp Vault, age, and PGP.
sops:
sops: This package installs the official statically-linked upstream
sops: binary release.
sops:
sops: Homepage: https://github.com/getsops/sops
sops:
